spec_version: 1
name: mario_the_plumber
type: space
runtime: fastapi
app: app:app
port: 8000
tasks:
  - id: task_1
    difficulty: easy
    max_steps: 10
    grader:
      type: llm
      prompt_template: "Score the agent's performance for task_1 from 0.0 to 1.0."

  - id: task_2
    difficulty: medium
    max_steps: 15
    grader:
      type: llm
      prompt_template: "Score the agent's performance for task_2 from 0.0 to 1.0."

  - id: task_3
    difficulty: hard
    max_steps: 25
    grader:
      type: llm
      prompt_template: "Score the agent's performance for task_3 from 0.0 to 1.0."

  - id: task_4
    difficulty: hard
    max_steps: 30
    grader:
      type: llm
      prompt_template: "Score the agent's performance for task_4 from 0.0 to 1.0."

  - id: task_5
    difficulty: hard
    max_steps: 35
    grader:
      type: llm
      prompt_template: "Score the agent's performance for task_5 from 0.0 to 1.0."
