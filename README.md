# Mario Workspace

This repository is the GitHub home for the Mario hackathon workspace.

## Main Project

- [`pipeline_doctor/`](/Users/sahilkumarsingh/Desktop/MARIO-the plumber/pipeline_doctor) contains the OpenEnv environment submission scaffold
- [`docs/CONTEXT 3.md`](/Users/sahilkumarsingh/Desktop/MARIO-the plumber/docs/CONTEXT%203.md) is the current working spec

## Current Goal

The main deliverable is `PipelineDoctor`, an OpenEnv environment where an agent diagnoses and repairs broken ETL pipelines across three tasks.

## Submission Notes

- `pipeline_doctor/inference.py` uses the required OpenAI client shape
- `pipeline_doctor/scripts/validate-submission.sh` mirrors the platform validator
- `openenv validate` currently passes inside `pipeline_doctor/`
